{
  "name": "logmein",
  "version": "0.0.1",
  "description": "poc for logmein rescue integration widget",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf build",
    "postinstall": "npm run compile",
    "compile": "npm run clean && babel src -d build",
    "start": "node ./build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^7.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "dotenv": "^5.0.0"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "body-parser-xml": "^1.1.0",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "html": "^1.0.0"
  }
}
