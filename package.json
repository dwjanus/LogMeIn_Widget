{
  "name": "samanage-widgets",
  "version": "0.1.0",
  "description": "poc for samanage platform integrations",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf build",
    "postinstall": "npm run compile",
    "compile": "npm run clean && babel src -d build",
    "start": "node ./build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^7.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "dotenv": "^5.0.0"
  },
  "dependencies": {
    "bluebird": "^3.5.1",
    "body-parser": "^1.18.2",
    "body-parser-xml": "^1.1.0",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "hogan-express": "^0.5.2",
    "lodash": "^4.17.10",
    "monk": "^6.0.5",
    "querystring": "^0.2.0",
    "request": "^2.85.0"
  }
}
